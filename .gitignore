node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Real env files (private keys, Cognito secret, AWS, Meta, DB). Never commit these.
.env
.env.*
!.env.example

# Runtime tenant data, sessions, and local secrets — never commit
.data/**
!.data/.gitkeep
sessions.json
*.pem
*.p12
credentials.json
service-account*.json

*.zip
scratch/
