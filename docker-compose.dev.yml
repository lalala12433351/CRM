# ==============================================================================
# Pixbe CRM - Development Docker Compose Setup
# Live code reloading (Vite HMR & TSX server) with PostgreSQL
# ==============================================================================

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: pixbe-crm:dev
    container_name: pixbe-crm-dev-app
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - NODE_ENV=development
      - PORT=8080
      - APP_URL=${APP_URL:-http://localhost:8080}
      - DB_HOST=${DB_HOST:-db}
      - DB_PORT=${DB_PORT:-5432}
      - DB_NAME=${DB_NAME:-postgres}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      - DB_SSL=${DB_SSL:-false}
      - PIXBE_STORE=${PIXBE_STORE:-postgres}
      - CONTROL_DB_NAME=${CONTROL_DB_NAME:-pixbe_control}
      - ADMIN_DB_NAME=${ADMIN_DB_NAME:-${AWS_RDS_DATABASE:-postgres}}
      - PIXBE_AUTO_IMPORT_JSON=${PIXBE_AUTO_IMPORT_JSON:-true}
      - AWS_RDS_HOST=${AWS_RDS_HOST:-}
      - AWS_RDS_PORT=${AWS_RDS_PORT:-}
      - AWS_RDS_DATABASE=${AWS_RDS_DATABASE:-}
      - AWS_RDS_USER=${AWS_RDS_USER:-}
      - AWS_RDS_PASSWORD=${AWS_RDS_PASSWORD:-}
      - AWS_RDS_SSL=${AWS_RDS_SSL:-}
      - COGNITO_REGION=${COGNITO_REGION:-ap-south-1}
      - COGNITO_USER_POOL_ID=${COGNITO_USER_POOL_ID:-}
      - COGNITO_CLIENT_ID=${COGNITO_CLIENT_ID:-}
      - COGNITO_CLIENT_SECRET=${COGNITO_CLIENT_SECRET:-}
      - COGNITO_DOMAIN=${COGNITO_DOMAIN:-}
      - COGNITO_REDIRECT_URI=${COGNITO_REDIRECT_URI:-http://localhost:8080/}
      - ALLOW_LEGACY_ADMIN=${ALLOW_LEGACY_ADMIN:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - META_APP_ID=${META_APP_ID:-}
      - META_APP_SECRET=${META_APP_SECRET:-}
      - META_WEBHOOK_VERIFY_TOKEN=${META_WEBHOOK_VERIFY_TOKEN:-}
      - META_REDIRECT_URI=${META_REDIRECT_URI:-}
    volumes:
      # Mount host source code into container for instant live reload
      - ./src:/app/src
      - ./server:/app/server
      - ./public:/app/public
      - ./index.html:/app/index.html
      - ./vite.config.ts:/app/vite.config.ts
      - ./tsconfig.json:/app/tsconfig.json
      # Prevent host node_modules from overwriting container node_modules
      - /app/node_modules
      # Persist local CRM data
      - pixbe_dev_data:/app/.data
    # Local Postgres is optional. RDS is used when AWS_RDS_HOST is set.
    depends_on:
      db:
        condition: service_healthy
        required: false
    networks:
      - pixbe-dev-network

  db:
    profiles: ["local-db"]
    image: postgres:16-alpine
    container_name: pixbe-crm-dev-db
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: ${DB_NAME:-postgres}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    volumes:
      - pixbe_dev_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - pixbe-dev-network

volumes:
  pixbe_dev_data:
    driver: local
  pixbe_dev_postgres_data:
    driver: local

networks:
  pixbe-dev-network:
    driver: bridge
